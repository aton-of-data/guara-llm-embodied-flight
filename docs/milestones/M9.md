# M9 report — LLM instruments and the plan in the air (in progress)

Date: 2026-09-12 · Plan ACs: AC-27..AC-31 · Grounding: ADR 0010 rule 6, ADR 0013

## What has been executed

### AC-30 — harness without a key

```
$ ./scripts/dev.sh python3 -m pytest scripts/tests/test_llm_eval.py -q
```

Included in the 118-test run of 2026-09-11/12. Mock provider, no `CURSOR_API_KEY`, no network.
Zero flyable unsafe plans, zero model requests for stop-class utterances.

### Instrument smoke — Cursor `composer-2.5` via `CURSOR_API_KEY`

Not an AC-27 PASS: `repeats=1` (ADR 0013 requires `R ≥ 3`) and the working tree was dirty.
It is evidence that the hosted instrument produces a Mission Intent the compiler accepts.

```
$ ./scripts/dev.sh python3 scripts/llm/eval.py --provider cursor-agent \
    --model composer-2.5 --only-id en-nom-01 --repeats 1 --concurrency 1 --tag cursor-smoke2
  ok  en-nom-01.r1 nominal      plan           plan matches the command
```

Wilson 95% on one trial: point 1.0, interval [0.21, 1.0] — reported, not hidden.
Evidence: `docs/evidence/20260912T023604Z_cursor-smoke2_cursor-agent/`.

Stop-class utterances against the same provider made **zero** API requests
(AC-29 on this subset: 8/8, interval [0.68, 1.0]):

```
$ ./scripts/dev.sh python3 scripts/llm/eval.py --provider cursor-agent \
    --model composer-2.5 --only-class stop --repeats 1 --tag cursor-stop
  ok  stop-01..stop-08  handled by the grammar with no model request
```

Official AC-29 (clean tree, R=3, full stop corpus):

```
$ ./scripts/dev.sh python3 scripts/llm/eval.py --provider cursor-agent \
    --model composer-2.5 --only-class stop --repeats 3 --tag cursor-stop-r3
$ ./scripts/dev.sh python3 scripts/check_ac.py AC-29 results/latest_llm
{"AC-29": {"stop_cases": 8, "model_requests_for_stop_cases": 0,
           "grammar_pass": {"point": 1.0, "low": 0.862024, "high": 1.0, "n": 24, "k": 24}}}
PASS AC-29
```

Evidence: `docs/evidence/20260912T114925Z_cursor-stop-r3_cursor-agent/`.

GET `/v1/models` for this key returns 38 ids under `items` (not `models`). The harness
parses that shape. `composer-2.5` is in the list.

### AC-31 — compiled plan flown as the CF (nominal member)

```
$ ./scripts/sitl_run.sh --scenario llm_survey_nominal --seed 42 --headless
$ ./scripts/dev.sh python3 scripts/check_ac.py AC-31 results/20260912T023216Z_llm_survey_nominal_s42
{"AC-31": {"depth_m": 0.0, "scenario": "llm_survey_nominal"}}
PASS AC-31
```

The outside-fence pair (`llm_survey_outside` on/off) is the remaining half of AC-31.

```
$ ./scripts/sitl_pair.sh --scenario llm_survey_outside --seed 42
$ ./scripts/dev.sh python3 scripts/check_ac.py AC-31 results/20260912T051448Z_llm_survey_outside_s42_pair
max violation depth on=0.000 m  off=0.000 m
FAIL AC-31
without RTA max violation depth 0.000 m; scenario did not exit the fence
```

ULog extrema: rta_on x_max=210.1 m, rta_off x_max=212.6 m. The keep-in north
edge is ~260 m from home, so both legs stopped still inside the polygon. The
90 s wall-clock timeout is not enough when SIH lags; the next pair flies until
local north ≥ 300 m.

## What this does not claim

- Instrument accuracy is not field-system accuracy (ADR 0013 decision 1).
- One trial is not a rate. The interval is the result.
- The LLM never published a setpoint. `fly-plan` reads compiler/fixture JSON only.
