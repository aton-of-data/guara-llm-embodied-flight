# M4 report — Geofence predictor

Date: 2026-09-11 · SPEC ACs: AC-8, AC-9 · Grounding: A.8, ADR 0004

## Plan (as executed)

1. Allocation-free predictor (`guara_geofence`) with analytic gtest `predictor_analytic`.
2. `GeofenceChannel` projects lat/lon vertices into the local NED frame using `ref_lat`/`ref_lon`
   (degrees, `vehicle_local_position_v1`) and feeds `T_gf` into `DecisionCore`.
3. SITL pair `gf_straight_concave` seed 42: the same CF +North velocity into a concave
   keep-in notch, with the geofence channel on vs off.

## Acceptance criteria

| AC | status | command | output excerpt |
|---|---|---|---|
| AC-8 | PASS | `./scripts/dev.sh colcon test --packages-select guara_geofence --ctest-args -R predictor_analytic` | `[  PASSED  ] 15 tests.` / `1/1 Test #1: predictor_analytic ... Passed` |
| AC-9 | PASS | `./scripts/sitl_pair.sh --scenario gf_straight_concave --seed 42 && ./scripts/dev.sh python3 scripts/check_ac.py AC-9 results/latest_pair` | pair `rta_on=20260911T192243Z_gf_straight_concave_s42_rta_on` / `rta_off=20260911T192404Z_gf_straight_concave_s42_rta_off`; `max violation depth on=0.000 m  off=9.999 m`; `PASS AC-9` |

The on-run arbiter log shows `T3 CF/NONE -> RF/HOLD cause=0x2` (geofence). The off-run stays in CF
until land (`T1`). Overlay `config/gf_straight_concave.yaml` sets `geofence.enabled: true` and
`core.tau_gf_s: 6.0`. The off member passes `--param geofence.enabled:=false`.

A first `sitl_pair.sh` attempt aborted on `wait-nav` (PX4 stayed in AUTO_LOITER). The two members
above were then run with the same flags the pair script uses; `results/latest_pair` points at that
pair. The runner now retries owned-mode `SET_NAV_STATE` once.

## Open risks

- `a_brake` remains [HYPOTHESIS]; AC-22 still needs a measured SIH deceleration after Hold.
- Channel is off by default (`geofence.enabled: false`) so the arbiter does not require a polygon
  until a scenario loads one.

## Next steps

- M7 latency aggregation (AC-18/19/22).
