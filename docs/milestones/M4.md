# M4 report — Geofence predictor (analytic path)

Date: 2026-09-11 · SPEC ACs: AC-8, AC-9 · Grounding: A.8, ADR 0004

## Plan (as executed)

1. Allocation-free predictor (`guara_geofence`) with analytic gtest `predictor_analytic`.
2. `GeofenceChannel` projects lat/lon vertices into the local NED frame using `ref_lat`/`ref_lon`
   (degrees, `vehicle_local_position_v1`) and feeds `T_gf` into `DecisionCore`.
3. AC-9 pair scenario (`gf_straight_concave`) is not yet implemented.

## Acceptance criteria

| AC | status | command | output excerpt |
|---|---|---|---|
| AC-8 | PASS | `./scripts/dev.sh colcon test --packages-select guara_geofence --ctest-args -R predictor_analytic` | `[  PASSED  ] 15 tests.` / `1/1 Test #1: predictor_analytic ... Passed` |
| AC-9 | OPEN | `sitl_pair.sh` / `gf_straight_concave` do not exist | — |

## Open risks

- `a_brake` remains [HYPOTHESIS]; AC-9 and AC-22 need a measured value in SIH.
- Channel is off by default (`geofence.enabled: false`) so the arbiter does not require a polygon until a scenario loads one.

## Next steps

- Scenario pair with/without RTA (AC-9).
- Measure `a_brake` from ULog deceleration after Hold.
