# M7 report — Latency budget

Date: 2026-09-11 · SPEC ACs: AC-18, AC-19, AC-22 · Grounding: A.8–A.10, §4.3

## Plan (as executed)

1. `RtaEvent` / `RtaState` carry L0 (`timestamp − timestamp_sample`), ROS−PX4 clock error,
   monitor L2/L3, and command/ACK times. Scenario `latency_hold` injects a switch-class
   verdict after owned CF, same path as AC-7.
2. Batch: `./scripts/sitl_batch.sh --runs 29 --seed-start 2` after a seed-1 pilot, 30 members
   in `results/batch_latency/runs.txt`, `fail=0`.
3. `python3 scripts/aggregate.py results/batch_latency` writes `metrics.json`. All numbers
   below are from that file.

## Acceptance criteria

| AC | status | command | output excerpt |
|---|---|---|---|
| AC-18 | PASS | `./scripts/dev.sh python3 scripts/aggregate.py results/batch_latency && ./scripts/dev.sh python3 scripts/check_ac.py AC-18 results/batch_latency` | `n_runs=30 complete_delta=30`; `delta_lat n=30 p50=0.04210844386676581 p99=0.06843425536311269`; `PASS AC-18` |
| AC-19 | PASS | `./scripts/dev.sh python3 scripts/check_ac.py AC-19 results/batch_latency` | `abs_p99_s=5.037156910228729` `limit_s=0.01` `cross_process_reported=false`; `PASS AC-19` |
| AC-22 | PASS | `./scripts/dev.sh python3 scripts/check_ac.py AC-22 results/batch_latency` | `delta_lat_p99_s=0.06843425536311269` `tau_rec_p99_s=0.4408000000000001` `geofence_satisfied=true` `daa_satisfied=true`; `PASS AC-22` |

Stage p50 / p99 from `results/batch_latency/metrics.json` (seconds):

| Stage | p50 | p99 | Clock |
|---|---|---|---|
| L0 | 0.0 | 0.004000 | PX4 `timestamp − timestamp_sample` |
| L1 | 3.614 | 4.566 | ROS recv − PX4 timestamp (**not** in `δ_lat`) |
| L2 | 0.0 | 0.0 | injected verdict (`processing_s=0`) |
| L3 | 0.000620 | 0.000801 | ROS stamp → arbiter recv |
| L4 | 0.01185 | 0.02863 | arbiter steady, recv → T3 |
| L5 | 0.01609 | 0.03168 | arbiter steady, publish → ACK (`L5_ack`) |
| L6 | 0.012 | 0.012 | ULog `vehicle_command` → `nav_state=AUTO_LOITER` |
| `δ_lat` | 0.04211 | 0.06843 | L0+L2+L3+L4+L5+L6 (L1 gated by AC-19) |

## Clock alignment (AC-19)

PX4 `timestamp` and the ROS system clock differ by p99 **5.037 s** on this SIH/uXRCE setup,
above the 10 ms hypothesis. Mixed-clock L1 is therefore stored as a stage but excluded from
`δ_lat` (SPEC §4.3). L5 uses ACK minus publish on the arbiter steady clock, not ULog minus ROS.

## Obligation O-1 (AC-22)

Canonical parameters `τ_gf = 1.0 s`, `τ_daa = 30.0 s`. Measured `δ_lat` p99 = 0.06843 s;
`τ_rec` p99 = 0.4408 s (ULog: time after AUTO_LOITER until speed < 0.5 m/s).

- Geofence: `1.0 ≥ 0.06843` — satisfied.
- DAA: `30.0 ≥ 0.4408 + 0.06843` — satisfied.

`τ_rec` here is Hold stopping time in hover, not a traffic-avoidance maneuver; DAA O-1 with
this RF remains a Hold-stop margin.

## Open risks

- L2 is zero because `latency_hold` injects the monitor; Copilot processing is not on this path.
- L0 is often zero in SIH (`timestamp == timestamp_sample`).
- Cross-process DDS latency is not a published number until PX4↔ROS alignment is < 10 ms.
