# M2 report — Copilot monitor via Ogma on `/fmu/out/*` (best-effort QoS)

Date: 2026-09-11 · SPEC ACs: AC-3, AC-3b · Grounding: 4.3–4.6, A.15

## Plan (as executed)

1. Add formal-methods image `docker/Dockerfile.fm` (Ogma v1.15.0, Copilot v4.8.1, FRET sources).
2. Copy Ogma's ROS template and patch subscriptions to `rclcpp::SensorDataQoS()` [G 4.5].
3. Write failing AC-3b test (`qos_profile`) against the stock depth-10 template, then apply the patch.
4. Specify REQ-ALT-01 (NED `z` non-negative) in Ogma's JSON format; generate Copilot C99.
5. Wrap generated `step()` in a ROS node that publishes `guara_msgs/MonitorVerdict`.
6. AC-3 SITL: scenario `alt_ceiling_violation` starts the monitor node, records verdicts, takeoff.

## Acceptance criteria

| AC | status | command | output excerpt |
|---|---|---|---|
| AC-3b | PASS | `./scripts/dev.sh colcon test --packages-select guara_monitors --ctest-args -R qos_profile` | `1/1 Test #1: qos_profile ... Passed` / `[  PASSED  ] 1 test.` |
| AC-3 | PASS | `./scripts/sitl_run.sh --scenario alt_ceiling_violation --seed 42 --headless && python3 scripts/check_ac.py AC-3 results/latest` | `PASS AC-3`; `1728` verdicts, first line `{"monitor_id": "REQ-ALT-01", "violated": true, ...}` |

Unit check of generated C (not an AC): `copilot_alt` — non-negative `z` does not fire; `z < 0` fires `handlerAltitudeBelowCeiling`.

SIH reports a slightly negative `z` on the ground, so REQ-ALT-01 can fire before takeoff. That still demonstrates PX4 → DDS → Copilot → `MonitorVerdict` with best-effort QoS. A sourced numeric ceiling remains [PARAMETER TBD].

## Defect found and fixed

- Ogma binary in the FM image was installed with `install` of the executable only, so
  `Paths_ogma_core` still expected `/root/.cabal/share/.../ogma-core-1.15.0/data/variable-db.json`.
  Generation stages those data-files before calling `ogma` (`scripts/generate.sh`); `Dockerfile.fm`
  copies them at image build time for the next rebuild.
- Copilot rejected `input_signal > -1.5` (mix of infix `>` and prefix `-`) and
  `input_signal + 1.5 > 0` (`(+)` not the Float stream operator in this spec's imports).
  REQ-ALT-01 uses `input_signal >= 0` (no climb). A numeric ceiling in metres is [PARAMETER TBD].

## Open risks

- AC-3 closed on SIH with seed 42; ground `z` is slightly negative so the smoke
  property fires immediately. A sourced altitude cap is still [PARAMETER TBD].
- REQ-ALT-01 is a smoke property, not a sourced regulatory limit.
- Generated C is committed; regenerating requires the FM image.

## Technical debt

- Ogma's generated ROS `copilot` package is discarded (`.ogma_out/`); the hand-written
  node publishes `MonitorVerdict` instead of `std_msgs/Empty`.
- `copilot_types.h` is empty; included only because Copilot emits the include.

## Next steps

- M3 SITL: drive Hold from a monitor verdict (AC-7).
