# M13c report — the first space-domain slice (in progress)

Date: 2026-09-12 · Plan ACs: AC-47, AC-48 · Grounding: ADR 0012, GROUNDING Addendum D
Guará SHA at execution: `696c4ed` (clean tree) · Host: `guara-dev:m1` via `scripts/dev.sh`

Rule O is satisfied (M7 latency and P4 batch are PASS), so space work is allowed to start
(ADR 0011 decision 2). This milestone is deliberately the *cheapest* first slice: geometry with
analytic tests and a closed intent vocabulary, before any F´ code and before any simulator.

## What has been executed

### AC-47 — attitude keep-out predictor, analytic cases

```
$ ./scripts/dev.sh python3 -m pytest scripts/tests/test_keepout_analytic.py -q
...........                                                              [100%]
11 passed in 0.01s

$ ./scripts/dev.sh python3 scripts/check_ac.py AC-47
...........                                                              [100%]
11 passed in 0.00s
PASS AC-47
```

Cases covered by the suite: boresight approaching the cone at a constant rate (closed-form time
to violation within ±0.05 s), receding, tangent, zero rate, already inside the cone (`T = 0`,
fail-closed), degenerate and non-finite inputs (`T = 0`), and no violation inside the horizon
(`+∞`). This is the AC-8 pattern the geofence predictor was held to, applied to a signal that
could not be ported from it: an orbiting body cannot stop, so there is no braking model
(ADR 0012, `SPACE-AUTONOMY.md` §5).

### Closed space-intent vocabulary

```
$ ./scripts/dev.sh python3 -m pytest scripts/tests/test_space_intent.py -q
........                                                                 [100%]
8 passed in 0.05s
```

`space/schema/space_intent.schema.json` admits `slew`, `point_hold`, `safe_mode`, `status`,
`abort` and nothing else — no quaternion, no F´ command name, no Fpy directive, no actuator
(risk RS-6). `space/intent.py` mirrors `mission/compiler/intent.py`: size cap, closed schema,
machine-readable rejection codes, and a `NOT_COMPILABLE` set that includes the stop class.

## What is **not** claimed

- **No F´ code exists.** M13 (shared-core extraction, the deployment, `Svc::Health`
  registration) has not started. `scripts/fprime.sh`, referenced by the M13–M15 acceptance
  criteria, is a deliverable of that milestone and does not exist yet.
- **No safe mode exists**, here or upstream (GROUNDING D.5, risk RS-1).
- **AC-48 has not been executed.** There is no simulator in the loop: Basilisk through the
  published ROS 2 bridge, driving the unmodified `guara_rta` node, is the next step
  (ADR 0012 decision 5).
- Every angle in `space/site/demo_sat.yaml` is **[PARAMETER TBD]**. The predictor is geometry
  with tests, not a statement about any spacecraft.
- The planar constant-rate closed form is exact for the analytic window and an approximation
  for a real slew profile. Any Basilisk-derived number will be reported bounded by the
  simulator's fidelity (risk RS-4).

## Next

| Step | Unblocks |
|---|---|
| Basilisk ↔ ROS 2 bridge evaluated, orbital run recorded under `results/` with the run contract | AC-48 |
| `attitude_keepout` as a channel in a space safety profile of the existing decision core | AC-48, and the first evidence that one core serves two domains |
| Shared-core extraction (`guara_core`) verified by re-running the PX4 suites unchanged | AC-39, then M13 |
